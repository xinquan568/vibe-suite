The vibe-suite auditor prepared fix PRs for this repository, but it is skipping
submission for now.

Repositories owned by **{{OWNER}}** require a signed Contributor License Agreement (CLA)
before external contributions can be accepted, and this project has no attestation on
file that the CLA covering our contributions has been signed. Submitting unsigned PRs
would only create noise for the maintainers, so the pipeline stops here instead.

Nothing is lost: the audit findings remain recorded, and this tracking issue stays open.

To re-enable contributions for this repository:

1. Sign the owner's CLA with the human contributor identity that will author the PRs.
2. Set the repository variables that attest the signature and identify the CLA-covered
   author (name and email).
3. Re-add the contribute-approval label to this issue — the contribute workflow will
   re-run and pick up from here.
