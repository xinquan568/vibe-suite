Row 9 is exercised by `tests/test_migrate_auditor_data.py` against a local bare-repo fixture,
shipped with E0.8. Duplicating it here would be a second opinion on a question that already has
an owner, so this row asserts that coverage exists rather than re-testing the migration.
