# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Node
node_modules/

# OS
.DS_Store

# Editors
*.swp

# vibe-suite runtime state (job records, runtime toggles)
.vibe-suite-state/

# E8.4 (vibe-61): site output generated by site/build.sh — never committed. The brand check
# therefore has to scan the BUILT directory, not just tracked files.
site/data/
site/reports/
site/dashboard.md
site/featured-audits.md
# Builder-rendered page trees. Each vibe-build-* tool writes an index here, so these are output,
# not source — the first staging attempt caught them entering the tracked tree.
site/reference/
site/vocab/
site/case-studies/
site/docs/
site/.vitepress/dist/
site/.vitepress/cache/
site/node_modules/
