# `commands/` — Slash commands

One markdown file per `/vibe-suite:*` command. Command frontmatter sets the bare
name; the `/vibe-suite:` prefix comes from `.claude-plugin/plugin.json:name` and is not set here.
Shared partials live in `commands/shared/`.
