---
description: Use when resolving build output paths for a plugin - covers the layout conventions, the plugin-root anchor, and what to do when the directory is absent.
---

Resolve output paths against the plugin root. When `build/` is absent, create nothing and report it.
