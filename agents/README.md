# `agents/` — Subagent definitions

One markdown file per agent, with frontmatter declaring its name, description
and allowed tools. Agents are dispatched by commands and skills, not invoked directly by users.
