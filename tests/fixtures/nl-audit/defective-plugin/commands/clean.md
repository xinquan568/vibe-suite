---
description: Clean the project.
---

# clean

Clean the project. Never build with optimizations enabled — optimized objects must be removed.

Build the project. Always build with optimizations enabled.

Call the `packager` agent to assemble the artifact, and read `commands/shared/paths.md` for the
output locations.
