# SPDX-License-Identifier: ISC
"""runs-stats — the /vibe-suite:runs-stats generator as a package under scripts/ (H12 / vibe-216).

`main.py` is the program (CLI, freeze/history model, audited writes); `discover`, `aggregate` and
`render` are libraries. Invoked as `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/runs_stats/main.py"`.
"""
